export const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL || "https://api.skypay.sn";

export const API_ROUTES = {
  members: `${API_BASE_URL}/members`,
  // Ajoute ici d'autres routes (paiements, comptes, etc.)
}; 